# 📜 Wedding Invitation - 사용자 질문 & 요청 히스토리 모음 (Prompt Log)

이 문서는 본 프로젝트 개발 과정에서 사용자가 안티그래비티에게 요청했던 모든 질문과 프롬프트를 시간순으로 정리한 로그입니다.  
다른 PC에서 동일한 질문을 다시 사용하거나, 과거 요청 내용을 복사/참조할 때 활용할 수 있습니다.

---

### 1차 요청: 커밋 메시지 마크다운 포맷팅
- 앞으로는 누적된 커밋 메시지를 마크다운으로 작성해서 붙여넣을 수 있게 해줘.
- 마크다운 언어의 기능을 이용해줘. 제목글은 진하게라던지 중요부분은 볼드 처리라던지.

---

### 2차 요청: 스크롤 효과 및 섹션 풀스크린
- 스크롤 할 때마다 각 섹션이 서서히 나타나도록 해줘.
- 청첩장 첫 섹션은 화면에 가득차게 해줘.
- **<현장 스냅>에서**:
  - 사용자들이 동영상을 올리면 재생되게 해줘.
  - 자동 스크롤에서 사용자들이 스크롤 할 수 있게 해줘.
- 마지막 섹션도 화면 꽉차게 해줘.

---

### 3차 요청: 상세 인터랙션 및 UX 고도화
- **<공통>**:
  - 스크롤 할 때 각 영역이 1/3 이상 보이면 서서히 나타나는 등장효과가 되도록 해줘.
  - 네비게이션 바에서 클릭할 때는, 스크롤 할 때마다 현재 어느 영역인지 가르키는 기능은 잠시 안되게 해줘. 네비게이션 바의 각 버튼을 클릭하면 스크롤할 때 현재 영역이 어디인지 가르키는 것 때문에 효과가 버벅여서 그래.
- **<첫번째 섹션>**:
  - 화면만 꽉차게 해서 컨텐츠가 너무 간격이 벌어져있어. 다시 조정해줘.
- **<갤러리 섹션>**:
  - 각 이미지를 클릭했을 때, 스토리 화면이 레이어로 꽉 안차게 돼있어. 레이어로 꽉차게 해줘.
  - 각 이미지를 누르고 있을 때 나타나는 미리보기 화면은 레이어 말고 뒷 배경이 흐려지는 효과로 해줘. 그리고 모바일 환경에서는 누르고 있으면 브라우저의 컨텍스트 메뉴가 뜨지 않도록 해줘.
- **<현장 스냅 섹션>**:
  - 자동 스크롤은 되게 하되, 사용자가 스크롤 하려고 하면 잠시 기능을 꺼주고, 한 5초 뒤에 자동 스크롤이 재개되도록 해줘.
  - 현재 각 이미지를 누르면 뜨는 레이어가 모바일 환경에서는 사용자가 스와이프하면 뒤 요소가 스크롤이 되고 있으니까 막아줘.
  - 샘플영상 파일 하나만 삽입해줘.
- **<맨마지막섹션>**:
  - 바로 위 섹션의 배경색과 일치해서 꽉차게 만든 것이 무의미해졌어. 구분감 있게 수정해줘.

---

### 4차 요청: 현장 스냅 배경 스크롤 안정화
- **<현장 스냅 섹션>**:
  - 스크롤 하면, 현장스냅 기능이 OFF일 때 뜨는 '이 자리에 여러분들이..'가 있는 영역이 같이 올라가. 안 올라가게 해줘. 그리고 뒤에 자동 스크롤이 동작을 안하고 있어. 되게끔 해줘.

---

### 5차 요청: 현장 스냅 일러스트 및 동영상 재생
- **<현장스냅 섹션>**:
  - 현장스냅 기능이 OFF됐을 땐, 사용자들이 스크롤해도 동작되지 않게 해줘.
  - 현장스냅 기능이 ON됐을 땐, 사용자가 올린 사진 이외에 예시 사진들은 갤러리의 사진들 말고 웨딩관련한 일러스트로 해줘.
  - 사용자가 올렸을 때, 업로드 성공한 확인창을 나가면 현장스냅 섹션이 보여지도록 해줘.
  - 사용자가 올렸을 때, 현장스냅 섹션을 보면 업로드한 이미지가 엑스박스로 나오고 있어. 바로 이미지가 올라간 효과가 나타나면서 이미지가 정상적으로 보이도록 해줘.
  - 현재 있는 영상을 클릭하면 아무것도 안되고 있어. 재생화면이 나오도록 해줘.
  - 영상 카드일 경우엔, 맛보기 영상이 재생되도록 해줘.
- **<맨 마지막 섹션>**:
  - 화면 꽉차게 말고 기본적으로 생각하는 푸터영역보다 약간만 크게 해줘.

---

### 6차 요청: 갤러리 썸네일 최적화 및 스크롤 인디케이터
- **<갤러리>**:
  - 이 섹션에 진입하면 스크롤 버벅임이 생기고 있어. 아마 이미지 사이즈가 커서 그런 것 같은데, 3x3 갤러리 영역의 이미지들은 섬네일로 표시해줘.
- **<현장스냅>**:
  - 스크롤 내려가는 건 무한정 잘되는데, 스크롤 올릴 때는 맨 위 스크롤이면, 스크롤 끝이라는 인디케이터 효과를 넣어줘. 그리고 스크롤되는 영역의 맨 위에 있는 서서히 흐려지는 디자인은 제거해줘.
  - 사용자가 올리지 않은 카드의 일러스트의 종류를 다양하게 해줘.

---

### 7차 요청: 모바일 뒤로가기 제어 및 팝업 화면 개선
- **<공통>**:
  - 첫번째 섹션에서 스크롤 했을 때, 컨텐츠가 있는 가운데로 스크롤이 오게끔 하는 건 없어도 될 거 같아. 스크롤 할 때마다 화면이 튀어.
  - 레이어로 뜨는 기능을 했을 때, 모바일 환경에서 뒤로가기를 하면 이전페이지로 가게 돼. 안가게끔 레이어가 떴을 때 이전페이지로 가지 않고, 레이어가 닫히도록만 해줘.
- **<현장스냅 영역>**:
  - 스크롤 영역에서 스크롤 할 때 맨 위로 갔을 때 진동효과는 없애줘.
  - 맨 아래로 스크롤이 갔을 때도 동일하게 인디케이터를 넣어줘.
  - 맨 위와 맨 아래로 스크롤이 갔을 때의 문구는 제거해주고, 끝으로 갔다는 표식만 나타나도록 해줘.
- **<갤러리영역>**:
  - 현재 레이어가 뜰 때, 섹션을 꽉 채우기만 하고, 브라우저 화면을 꽉 채우진 않고 있어. 고쳐줘.

---

### 8차 요청: 스토리 뷰어 재생/정지 제어 및 스켈레톤 로딩
- **<갤러리>**:
  - 레이어팝업을 닫을 때 왜 첫 섹션으로 스크롤 이동돼있는거야? 레이어팝업을 누르기 전의 위치를 레이어팝업에서 나가면 그대로 있게끔 해줘.
  - 스토리에서 쭉 누르고 있을 때 나타나는 '화면 정지'는 제거해줘.
  - 만약 스토리에서 사용자가 일시정지 버튼이 눌린 상태에서 다음 이미지로 넘어가면 다시 재생되게 해줘.
- **<현장스냅>**:
  - 닫기 버튼을 누르면 레이어팝업만 제거되는 게 아니고 about:blank로 가게 돼. 수정해줘.
  - 각 카드의 이미지가 느리게 로딩될 때 스켈레톤 UI 적용해서 로딩되고 있다는 걸 알려줘.

---

### 9차 요청: 갤러리 스크롤 위치 버그 해결 및 현장스냅 중복/동영상 수정
- <갤러리> 섹션에서의 레이어팝업을 나갈 때 스크롤이 들어가기 직전 스크롤 위치랑 달라져있어. <현장스냅> 섹션에서는 레이어팝업을 나가도 들어오기 직전의 스크롤 위치가 달라지지 않았는데, <갤러리> 섹션은 왜 그러는거야. 고쳐줘.
- 스켈레톤 로딩이 <갤러리> 섹션의 이미지에는 적용이 안된 것 같아. 적용해줘.
- <현장스냅> 섹션에서 사용자들이 영상이나 이미지를 클릭했을 때는, 자동 스크롤이 일시정지 되게 해줘.
- 영상을 업로드했는데, 영상이 재생이 안돼. 고쳐줘.
- 실시간으로 사용자들이 올리게 되면, 예시 이미지가 같은 이미지가 스크롤되면서 보여지고 있어. 안 겹치게 해줘.

---

### 10차 요청: 가이드라인 및 프롬프트 로그 기반 프로젝트 전수 점검 및 반영
- 지금 agent.md와 prompt log.md 파일을 읽어서 현재 프로젝트에 반영해줘.
- **점검 및 반영 사항**:
  - `AGENTS.md` 및 `PROMPTS_LOG.md` (1~9차)의 모든 요구사항과 인터랙션 규칙(스크롤 리빌, 모바일 히스토리 뒤로가기 제어, 갤러리 썸네일/스토리 뷰어 스켈레톤, 현장스냅 무한 롤링 및 일러스트 분리 풀 등) 대조 및 정밀 검증.
  - `<첫번째 섹션 (CoverSection)>`: 모바일 환경에서 풀스크린 레이아웃(`min-height: 100vh; min-height: 100dvh; justify-content: center;`)을 확실히 유지하도록 스타일 보강 완료.
  - `<빌드/개발 환경 호환성>`: Node v20.11 환경에서 최신 Vite 8/rolldown이 `util.styleText` 부재로 인해 실행 실패하던 문제를 해결하기 위해, Node 20.0+와 완벽히 호환되는 안정 버전(`vite: ^6.2.7`, `@vitejs/plugin-vue: ^5.2.4`)으로 조정하여 `npm run dev` 및 `npm run build` 정상 구동 보장.

---

### 11차 요청: 갤러리 스크롤/방향키/최적화, RSVP 자동하이픈, 현장스냅 노출관리/영상재생, 관리자 구글인증/드래그 즉각반응
- **<갤러리>**:
  - 스토리 창을 닫을 때 스크롤이 맨 위로 올라가는 현상 수정 (다중 타이머 및 fallback 위치 보장).
  - PC 브라우저 스토리 뷰어에서 중복 표시되던 내부 방향키 제거 (단일 네비게이션 버튼만 노출).
  - 썸네일 이미지 로딩 속도 및 래그 최적화 (lazy loading, fetchpriority="low", thumbnailUrl 지원).
- **<참석의사>**:
  - 핸드폰 번호 입력 시 번호만 입력하면 실시간으로 자동 하이픈('-')이 삽입되도록 개선.
- **<현장스냅>**:
  - 관리자 페이지에 청첩장 현장스냅 기능 자체를 보이기/숨기기(노출/비활성화)할 수 있는 토글 스위치 설정 추가.
  - 현장스냅 영상 재생 지원 (사운드 자동재생 차단 시 음소거 폴백 재생, playsinline 및 controls 보강).
- **<관리자페이지>**:
  - 관리자 비밀번호(PIN) 로그인 유지 + 인증된 Google 사용자(Google Sign-In) 원클릭 로그인 지원 및 허용 이메일 관리 기능 추가.
  - 갤러리 탭 순서 변경 드래그 시 딜레이 제거 (마우스 이동 2px 즉각 반응, 브라우저 기본 이미지 드래그 충돌 차단, 경량 고스트 썸네일 적용).

---

### 12차 요청: 갤러리 드래그 영역 확장, 창제목 변경, 구글로그인 에러 안내, 전화번호 공통 자동하이픈, 현장스냅 실시간 토글 버그 수정
- **<관리자페이지>**:
  - 갤러리 탭 순서 변경 드래그 가능 영역을 드래그 핸들뿐만 아니라 **이미지 전체 영역**까지 확장 (버튼 클릭 동작은 유지하며 빈 영역/이미지 어디든 드래그 가능하도록 개선).
  - 관리자 페이지 접속 시 브라우저 탭/창 제목을 `'청첩장 관리자 페이지'`로 동적 변경 적용 (`vue-router` 메타 타이틀 및 라이프사이클 동기화).
  - Google 로그인 시 `auth/configuration-not-found` 오류에 대해 원인(Firebase Authentication에 Google 공급자 미활성화)과 콘솔 활성화 해결 방법을 직관적인 한글 메시지로 안내.
- **<공통>**:
  - 프로젝트 내 모든 전화번호 입력란(신랑 연락처, 신부 연락처, 예식장 대표 번호, RSVP 참석자 연락처)에 숫자만 입력해도 하이픈(`-`)이 실시간 자동 삽입되도록 공통 포맷터(`src/utils/format.ts`) 적용 (02 서울, 010/011 휴대폰, 031/051 지역번호, 1588 전국대표번호 모두 지원).
- **<현장스냅>**:
  - 관리자 페이지에서 현장스냅 보이기/숨기기를 실시간으로 껐다 켰을 때 청첩장에서 화면이 투명해지던 버그 해결 (`v-show` 기반 전환 + `adminSettings.showLiveSnapSection` 감지 시 IntersectionObserver 재등록 및 `.is-visible` 상태 즉시 동기화, 자동 스크롤/비디오 재생 자동 재개).

---

### 13차 요청: 갤러리/현장스냅 닫기(뒤로가기) 팝업 분리 및 갤러리 첫 화면 스켈레톤 로딩 적용
- **<갤러리 & 현장스냅 닫기 팝업 분리>**:
  - 갤러리 스토리 뷰어와 현장스냅(라이트박스/업로드 모달)의 `popstate` 및 닫기 버튼 핸들러를 완전히 분리 격리.
  - 기존에는 하나의 팝업이 닫힐 때 다른 컴포넌트의 `handlePopState`도 동시에 호출되어 서로의 `savedScrollY` 복원 타이머가 충돌하며 스크롤 위치가 튀던 문제를 해결.
  - 현재 자신의 모달이 활성화된 경우에만 닫기 및 스크롤 복원(`restoreScrollPosition` / `restoreLiveSnapScrollPosition`)을 수행하고, 열려있지 않을 때는 일체 간섭하지 않도록 수정.
- **<갤러리 초기 스켈레톤 로딩>**:
  - 기존에 '더보기'를 클릭할 때만 스켈레톤이 보이고 첫 진입 시에는 나타나지 않던 문제를 해결.
  - `isInitialLoading` 상태(0.55초)를 도입하여 청첩장 최초 접속 시에도 3x3 썸네일 그리드에 골드 스피너/쉬머 스켈레톤 UI가 안정적으로 노출된 뒤, 이미지가 부드러운 페이드인(`opacity: 1`)으로 우아하게 나타나도록 개선.

---

### 14차 요청: 갤러리 PC 썸네일 7~9번 마우스오버 버그 해결 및 현장스냅 스크롤 체이닝/표식/그라디언트 디자인 개선
- **<갤러리>**:
  - PC 환경에서 갤러리 영역으로 스크롤했을 때 7~9번째 사진이 브라우저 lazy threshold 지연으로 인해 마우스를 올려야만 나타나던 버그 수정.
  - 초기 9장 썸네일에 대해 `loading="eager"` 및 `fetchpriority="high"` 적용과 함께 캐시된 이미지 완료 상태(`complete`) 즉시 감지 바인딩 추가로, 스크롤 진입 시 9장 전 사진이 즉각 온전히 노출되도록 개선.
- **<현장스냅>**:
  - 스크롤을 계속 내리거나 올릴 때 목록의 처음/끝 골드 라인 & 핍 표식이 한 번만 깜빡이고 사라지지 않고, 계속해서 조작을 시도하는 동안 지속적으로 유지되도록 타이머 갱신 로직 수정.
  - 피드 하단 영역의 서서히 흐려지던 디자인(`-webkit-mask-image`, `mask-image`)을 완전히 제거하여 전체 피드 카드가 또렷하게 보이도록 개선.
  - 스크롤 뷰포트에 `overscroll-behavior-y: auto` 및 경계 도달 시 부모 창 스크롤(`window.scrollBy`) 보장을 적용하여, 맨 처음 또는 맨 끝에 도달했을 때 바깥 페이지의 스크롤이 자연스럽게 이어지도록 연동 완료.

---

### 15차 요청: 갤러리 초기 6장 노출 & 접기 스크롤 이동, 현장스냅 5초 딜레이 자동재개 및 iOS 스프링 바운스
- **<갤러리>**:
  - 초기 화면 노출 장수를 9장에서 **6장(`INITIAL_COUNT = 6`)**으로 조정하여 모바일 화면에서 한눈에 깔끔하게 보이도록 개선.
  - '사진접기' 버튼 클릭 시 뷰포트가 어색하게 남지 않고 갤러리 섹션 상단(`gallery-section`)으로 부드럽게 스크롤 복원(`scrollIntoView({ behavior: 'smooth', block: 'start' })`).
- **<현장스냅>**:
  - 카드(사진/동영상) 클릭 시 자동스크롤이 즉시 멈추고, 컨텐츠 레이어 팝업이 닫히면 **정확히 5초 뒤에 자동스크롤이 활성화**되도록 지연 타이머(`resumeAutoScrollAfterDelay(5000)`) 구현.
  - 자동스크롤 영역에서 최상단/최하단 끝에 도달한 후 드래그/휠 스크롤 시도시, 콘텐츠 영역 전체가 경계를 넘어갔다가 손을 떼는 순간 탱글하게 되돌아오는 **iOS 스타일의 러버밴드 스프링 바운스(`cubic-bezier(0.175, 0.885, 0.32, 1.275)`)** 애니메이션 적용.

---

### 16차 요청: 오시는 길 네이버 지도 OpenAPI 전면 교체 및 관리자 페이지 API Key 실시간 설정 지원
- **<오시는 길>**:
  - 기존의 오픈스트리트맵 iframe을 완전히 제거하고 **네이버 지도(Web Dynamic Map v3)**로 전면 교체.
  - 정적 `.env`에 키를 넣고 재배포할 필요 없이, **관리자 페이지에서 네이버 지도 API Key(Client ID)를 직접 입력/수정/저장**할 수 있도록 구현.
  - `src/types/wedding.ts` 및 `initialData.ts`에 `naverMapClientId` 필드 추가 및 Firebase Firestore 실시간 클라우드 동기화 지원.
  - `InfoEditor.vue`(예식 정보 편집 탭) 및 `AdminSettings.vue`(환경 설정 탭) 양쪽 모두에서 직관적으로 키를 설정할 수 있도록 전용 입력 폼과 NCP 콘솔 바로가기 링크 배치.
  - 브라우저 클라이언트에서 키 유무를 감지하여 SDK를 비동기 동적 주입하는 **동적 스크립트 로더** 및 미등록 시 우아한 Fallback 안내 UI 구축.
  - 전역 TypeScript 타입 정의(`src/types/navermaps.d.ts`) 추가.

---

### 17차 요청: 네이버 지도 ncpKeyId 규격 적용(오류 해결), 지도 오버레이 제거, 내비 버튼 순서 및 앱스토어 공식 아이콘/배경색 일체화
- **<네이버 지도 ncpKeyId 규격 적용>**:
  - 지도가 뜨지 않던 핵심 원인이었던 파라미터 규격을 `ncpClientId`에서 공식 최신 규격인 **`ncpKeyId`**(`https://oapi.map.naver.com/openapi/v3/maps.js?ncpKeyId=YOUR_CLIENT_ID`)로 전면 개편.
  - `index.html`과 `LocationSection.vue`의 동적 로더 모두에 `ncpKeyId`를 바인딩하여 관리자 페이지에 설정한 API Key가 즉시 주입되도록 완성.
  - 결혼식장 위치 좌표(`venue.mapLat`, `venue.mapLng`)에 정확히 지도 핀 마커(`new naver.maps.Marker`) 렌더링.
- **<지도 영역 오버레이 제거>**:
  - 지도 캔버스 위에 떠 있던 상단 네이버지도 브랜드 뱃지(`.naver-brand-badge`) 및 하단 상록아트홀 길찾기 버튼(`.map-venue-tag`)을 완전히 제거하여 순수한 네이버 지도 화면만 넓고 깨끗하게 표시.
- **<내비게이션 버튼 순서 및 앱스토어 공식 아이콘/배경색 일체화>**:
  - 버튼 배치 순서 변경: **1. 네이버지도 ➔ 2. 카카오맵 ➔ 3. 티맵**
  - 각 버튼의 배경색을 앱스토어에 등록된 아이콘의 주된 공식 색상과 100% 일치하도록 개편:
    - **네이버지도 버튼**: 앱스토어 공식 **네이버 그린 (`#03C75A`)** 배경 + 화이트 텍스트 & 흰색 핀(그린 N 심볼) 아이콘
    - **카카오맵 버튼**: 앱스토어 공식 **카카오 옐로우 (`#FEE500`)** 배경 + 카카오 다크 (`#191919`) 텍스트 & 공식 말풍선 핀 아이콘
    - **티맵 버튼**: 앱스토어 공식 **순백색 (`#FFFFFF`)** 배경 + 다크 텍스트 + TMAP 3D 도로 루프 'T' 공식 입체 그라데이션 아이콘
---

### 18차 요청: 네이버 지도 "인증 실패" 원인 정밀 진단 및 원천 해결
- **<인증 실패 원인 1: index.html 정적 미치환 스크립트 제거>**:
  - `index.html`에 존재하던 `<script src="...ncpKeyId=%VITE_APP_NAVERMAP_KEY%">` 태그가 페이지 초기 진입 시 빈 키로 실행되어 네이버 API 서버에 즉시 인증 실패(Alert/에러)를 발생시키는 버그 해결 (정적 태그 제거 후 `LocationSection.vue`에서 관리자 설정 키로만 동적 주입).
- **<인증 실패 원인 2: ncpKeyId & ncpClientId 듀얼 파라미터 규격 적용>**:
  - `https://oapi.map.naver.com/openapi/v3/maps.js?ncpKeyId=${key}&ncpClientId=${key}` 형태로 두 파라미터를 동시에 제공하여 NCP 신구 규격 및 어떤 파서에도 100% 대응되도록 로직 강화.
- **<인증 실패 원인 3: NCP 콘솔 Web 서비스 URL 등록 가이드 & 원클릭 복사 도구 제공>**:
  - 네이버 지도는 NCP Application에 등록된 도메인(Web 서비스 URL)에서만 작동함(로컬 `http://localhost:5173` 및 배포 도메인).
  - `InfoEditor.vue` 및 `AdminSettings.vue`에 현재 브라우저 주소(`window.location.origin`)를 원클릭으로 복사할 수 있는 도구와 함께 "네이버 인증 실패 해결 3단계 체크리스트"를 시각적으로 추가.
- **<지도 렌더링 캔버스 크기 안정화>**:
  - 지도 인스턴스 마운트 시 `window.naver.maps.Event.trigger(map, 'resize')` 트리거를 적용하여 캔버스 타일 로딩 누락 방지.
---

### 19차 요청: 캘린더 등록 기능 제거, 카카오맵 앱스토어 블루 핀 아이콘 반영, 참석의사(RSVP) 노출 토글 및 동적 교차 배경색 시스템
- **<캘린더 일정 등록 기능 제거>**:
  - `CalendarSection.vue`에서 "캘린더에 일정 등록하기" 버튼, 하단 팝업 모달, ics 파일 생성 및 구글/네이버 캘린더 연동 함수와 관련 CSS 전면 제거.
- **<카카오맵 앱스토어 공식 아이콘 반영>**:
  - `LocationSection.vue`의 카카오맵 버튼 내 심볼을 카카오톡 말풍선에서 **앱스토어 공식 카카오맵 블루(#0066FF) 위치 핀 (중앙 펀칭 홀 구조)** 심볼로 교체하여 카카오 옐로우 배경과 완벽 일치시킴.
- **<참석의사전달(RSVP) 섹션 보이기/감추기 기능 추가>**:
  - `weddingInfo.showRsvp` 및 `adminSettings.showRsvpSection` 모델 확장.
  - 관리자 환경 설정(`AdminSettings.vue`)에 "청첩장 섹션 노출 관리" 카드 추가 및 실시간 토글 지원.
  - 참석여부 명단 조회(`RsvpViewer.vue`) 상단에도 실시간 "청첩장 섹션 노출" 스위치 토글 추가.
  - 토글 시 `localStorage` 및 Firebase Firestore 실시간 클라우드 동기화 지원.
- **<섹션 동적 교차 배경색 겹침 방지 시스템 (`InvitationView.vue`)>**:
  - 특정 섹션(RSVP, 현장스냅 등)이 숨겨졌을 때 이전/다음 섹션의 배경색(White, Ivory)이 동일하게 겹치는 문제 원천 해결.
  - 각 섹션 컴포넌트의 루트 배경색을 `var(--section-bg, ...)`로 유연화.
  - `InvitationView.vue`에서 현재 화면에 실제로 렌더링되는 가시 섹션 목록(`middleSectionThemes`)을 실시간 계산하여 **가시 순서에 따라 White ➔ Ivory ➔ White ➔ Ivory가 무조건 번갈아 교차 적용**되도록 구현.
  - 하단 네비게이션도 인덱스 기반에서 `data-section` 속성 기반으로 고도화하여 어떤 섹션이 빠지더라도 정확한 위치로 스크롤 및 활성화 매핑 보장.

---

### 20차 요청: 방명록 실시간 동기화 & 비속어 필터, 갤러리 1:1 얼굴 초점 크롭, 관리자 노출 토글 디자인 통합
- **<방명록 관리 실시간 삭제 및 노출/미노출 반영 (`GuestbookModerator.vue`, `storage.ts`)>**:
  - `storage.ts`에 `toggleGuestbookVisibility(id)` 함수를 추가하여 로컬 상태뿐만 아니라 Firestore `saveGuestbookDoc(item)`을 즉시 실행.
  - `deleteGuestbookEntry` 삭제 시 로컬스토리지 갱신 및 Firestore `deleteGuestbookDoc(id)` 실시간 발행.
  - `subscribeGuestbook` 구독 콜백의 배열 길이 가드를 완화하여 모든 데이터 삭제나 상태 변경 시 즉시 모든 하객 및 관리자 화면에 실시간 동기화되도록 개선.
- **<방명록 섹션 카드 배경색 동적 대비 전환 (`InvitationView.vue`, `GuestbookSection.vue`)>**:
  - `InvitationView.vue`에서 방명록 섹션의 배경색(White vs Ivory)에 맞춰 반대되는 `--card-bg` 및 `--input-bg` CSS 변수를 하위로 주입.
  - 방명록 작성 카드(`.write-card`), 입력창(`.input-field`), 방명록 피드 카드(`.comment-card`)가 섹션 배경색 전환 시 반대색으로 자동 전환되어 최적의 시각적 가독성과 대비 유지.
- **<방명록 비속어 및 악담 입력 방지 필터링 (`filter.ts`, `GuestbookSection.vue`)>**:
  - 욕설, 비하어, 음란 단어 및 결혼식에 부적절한 악담/저주/비방 키워드를 감지하는 `checkProfanity` 유틸리티 함수 구현 (`src/utils/filter.ts`).
  - 공백이나 특수문자로 우회하는 경우(`시.발`, `이 혼`, `개-새-끼` 등)까지 정규식으로 감지.
  - 방명록 제출 시 비속어 감지 시 친절하고 정중한 안내창과 함께 저장을 자동 차단.
- **<갤러리 1:1 정사각형 썸네일 얼굴 초점 위치 맞춤 저장 (`PhotoItem`, `PhotoManager.vue`, `GallerySection.vue`)>**:
  - `PhotoItem` 모델에 `objectPosition?: string` (예: `50% 20%`) 필드 추가.
  - 관리자 사진 관리 탭에 **"1:1 썸네일 위치 맞춤" 인터랙티브 모달** 구현.
    - 원본 사진 위에서 클릭/터치/드래그하여 신랑/신부의 얼굴 위치를 타겟팅(십자선 레티클 및 펄스 링 UI).
    - [얼굴/상단 (20%)], [상단-중간 (35%)], [정중앙 (50%)], [하단 (75%)] 원클릭 프리셋 및 미세조정 슬라이더 제공.
    - 청첩장 3x3 갤러리에 노출될 **실제 1:1 정사각형 썸네일 실시간 라이브 미리보기** 제공.
    - 사진 정보 수정 모달 및 사진 카드 오버레이/액션바에서 간편 접근 지원.
    - `GallerySection.vue`의 3x3 썸네일에 `:style="{ objectPosition: photo.objectPosition || 'center center' }"` 바인딩을 적용하여 잘림 없는 최적의 구도 완성.
- **<관리자 참석여부 & 현장스냅 섹션 노출 토글 디자인 통합 (`RsvpViewer.vue`, `LiveSnapManager.vue`)>**:
  - `RsvpViewer.vue`와 `LiveSnapManager.vue`의 제목(`<h3>`) 바로 뒤에 `[청첩장에 노출 (ON/OFF)]` 스타일의 미니 알약(Pill) 토글 스위치 디자인을 통일하여 배치.
  - 스위치 변경 시 즉시 로컬 저장 및 Firestore 클라우드 동기화 트리거 연동.
- **<예식 일시/장소 탭 "네이버 인증 실패" 안내 박스 정리 (`InfoEditor.vue`)>**:
  - `InfoEditor.vue`에서 중복 노출되던 💡 "네이버 인증 실패" 해결 안내 박스 및 관련 미사용 함수/스타일 깔끔하게 제거 (`AdminSettings.vue`의 정규 안내 박스로 통합).

---

### 21차 요청: 연락처 보기 모달 팝업 잘림 해결 (Teleport & Popstate) 및 현장스냅 중복 노출 토글 정리
- **<연락처 보기 모달 팝업 잘림 및 스태킹 컨텍스트 분리 (`GreetingSection.vue`)>**:
  - 인삿말 섹션(`GreetingSection.vue`)의 축하 연락처 모달을 `<Teleport to="body">`로 래핑하여 섹션 래퍼의 `opacity` / 스태킹 컨텍스트 및 후속 섹션(캘린더, 갤러리, 지도 등)에 의해 잘리거나 덮이는 현상 원천 해결.
  - 모달 오버레이 `z-index`를 `9999`로 상향 및 `-webkit-backdrop-filter` 적용.
  - 모달 오픈 시 배경 스크롤 잠금(`document.body.style.overflow = 'hidden'`) 처리.
  - 모바일 뒤로가기(`popstate`) 시 페이지 이탈 없이 연락처 모달만 닫히도록 `history.pushState` 연동 및 직전 스크롤 위치(`savedScrollY`) 오차 없는 복원 연동.
- **<관리자 현장스냅 탭 중복 청첩장 노출 스위치 제거 (`LiveSnapManager.vue`)>**:
  - 제목 바로 옆으로 통합된 `[청첩장에 노출 (ON/OFF)]` 알약 토글 스위치만 남기고, 하단 '현장 스냅 노출 및 테스트 모드' 카드 내부에 중복 노출되던 구형 상태 배너 스위치를 깔끔히 제거.
  - 카드 타이틀을 `예식 일자 및 현장 스냅 테스트 모드`로 정리하여 시각적 직관성 확보.

---

### 22차 요청: 오시는 길 지도 티맵(TMAP) 비모바일 비활성화 및 모바일 전용 안내 표기
- **<모바일 기기 환경 정밀 감지 (`LocationSection.vue`)>**:
  - `UserAgent` 정규식 검사(Android, iOS, CriOS 등) 및 터치 포인트/화면 너비 복합 검사를 통해 실제 스마트폰/모바일 환경 여부(`isMobileDevice`)를 정확히 판별.
  - 브라우저 창 크기 변경(`resize`) 시에도 실시간으로 반응하도록 이벤트 리스너 등록.
- **<비모바일(PC) 환경 티맵 버튼 비활성화 UI/UX>**:
  - PC 환경 감지 시 티맵 버튼에 `.is-disabled` 클래스 및 `:href="undefined"`, `aria-disabled="true"` 적용.
  - 비활성화 비주얼(그레이스케일, 투명도 58%, `cursor: not-allowed`, 호버 효과 차단) 적용 및 버튼 내 `[모바일 전용]` 서브 배지 표기.
- **<모바일 전용 안내 문구 및 인터랙티브 토스트 팝업>**:
  - 길찾기 3개 버튼 하단에 **"티맵(TMAP)은 스마트폰 앱 전용입니다. PC에서는 네이버지도 또는 카카오맵을 이용해 주세요."** 상시 안내 배너 표기.
  - PC 사용자가 티맵 버튼을 클릭했을 시, 길찾기 딥링크를 차단하고 **"티맵(TMAP)은 모바일 전용 기능입니다. 스마트폰에서 접속하시거나 PC에서는 네이버지도/카카오맵을 이용해 주세요."** 플로팅 토스트 팝업을 노출하여 친절하게 안내.

---

### 23차 요청: 청첩장 및 관리자 페이지 파비콘(Favicon) 맞춤 제작 및 동적 분기
- **<결혼식 청첩장 맞춤 파비콘 제작 (`public/favicon.svg`, `public/favicon-wedding.svg`)>**:
  - 소프트 아이보리 서클 베이스(`#FFFFFF` ~ `#F8F3EA`)에 샴페인 골드 테두리 적용.
  - 두 사람의 영원한 사랑과 결합을 상징하는 두 개의 연결된 골드 웨딩 링(Interlocking Gold Rings) 디자인.
  - 신부 링 상단에 빛나는 다이아몬드 젬스톤(Diamond Gem)과 주변 반짝임(Sparkle) 스파클 효과.
  - 링 교차 지점에 로맨틱한 로즈 핑크 하트 악센트 배치로 작은 탭 아이콘(16x16 / 32x32)에서도 선명하고 고급스럽게 표현.
- **<관리자 페이지 차별화 파비콘 제작 (`public/favicon-admin.svg`)>**:
  - 청첩장의 골드 웨딩 링 모티브를 계승하면서도 관리/설정 페이지임을 즉각 인지할 수 있도록 차별화.
  - 고급스러운 다크 네이비/슬레이트 원형 배경(`#1E2738` ~ `#0E131F`)에 골드 림 테두리.
  - 골드 웨딩 링 베이스와 함께 전면에 정교한 8개 톱니의 골드 기어(Admin Settings Gear) 배지 및 실시간 라이브 그린 인디케이터 도트 배치.
- **<HTML 메타 태그 및 동적 파비콘 라우터 연동 (`index.html`, `src/router/index.ts`)>**:
  - `index.html`에 `<link rel="icon" type="image/svg+xml" id="dynamic-favicon" href="./favicon.svg" />` 표준 태그 등록.
  - `src/router/index.ts`의 `afterEach` 훅에 `updateFavicon()` 유틸 함수를 구축:
    - `/admin` 경로 진입 시 `favicon-admin.svg`로 실시간 전환.
    - 일반 청첩장 메인(`/`) 진입 시 `favicon.svg` (웨딩 링)로 실시간 전환.
  - `scripts/sync-assets.cjs`에 파비콘 파일들을 루트(`root/`)와 빌드 산출물(`dist/`) 양쪽에 자동 동기화하도록 빌드 파이프라인 보완.

---

### 24차 요청: 갤러리 섹션 초기 표시 이미지 개수 9개(3x3) 확장
- **<갤러리 섹션 초기 노출 사진 수 9개로 상향 (`GallerySection.vue`)>**:
  - 기존 `INITIAL_COUNT = 6` (2행 6장)에서 `INITIAL_COUNT = 9` (3행 3열 9장)으로 변경.
  - 3열 그리드 레이아웃의 비어 있던 마지막 행을 채워 완전한 3x3 정사각형 그리드 완성.
  - 초기 9장 모두 고우선순위 로딩(`eager`, `fetchpriority="high"`) 적용.
  - 등록된 사진이 9개 이하일 때는 '사진 더보기' 버튼이 자연스럽게 숨겨지고, 9개 초과 시 남은 사진 개수(`remainingPhotosCount`)에 맞춰 더보기 버튼 노출.

---

### 25차 요청: 갤러리 롱프레스 픽(Peek) 모달 시 뒷 배경 스크롤 방지
- **<모바일 터치 타깃 캡처 및 터치 이동 스크롤 완벽 차단 (`GallerySection.vue`)>**:
  - 모바일 브라우저의 터치 이벤트 타깃 고정(Touch Target Locking) 특성으로 인해 텔레포트 오버레이가 생성되어도 최초 터치된 `.thumbnail-card`로 터치 이동 스트림이 계속 유입되는 문제 해결.
  - `handleThumbnailTouchMove`에서 `isPeeking.value === true` 상태일 때 `e.cancelable && e.preventDefault()`를 즉시 호출하여 브라우저의 제스처 스크롤 차단.
  - 롱프레스 인식 시점(320ms 경과)에 `document.body.style.overflow = 'hidden'` 적용 및 `window.addEventListener('touchmove', preventDefaultTouchMove, { passive: false })`를 브라우저 윈도우 최상위 레벨에 등록하여 손가락을 상하좌우로 움직여도 배경 div/페이지가 절대 스크롤되지 않도록 원천 잠금.
  - 롱프레스 종료(`handleThumbnailTouchEnd`, `closePeek`) 및 컴포넌트 언마운트(`onUnmounted`) 시 `body.overflow = ''` 및 `touchmove` 리스너를 완벽하게 해제/정리.

# Wedding Invitation Project - Antigravity Guidelines & Rules

이 문서는 다른 PC나 다른 세션에서 본 프로젝트(`wedding-invitation`)를 작업할 때 Antigravity AI가 자동으로 로드하여 일관된 작업 규칙과 사용자 요구사항을 준수하도록 정의한 프로젝트 가이드라인입니다.

---

## 1. 응답 및 커밋 메시지 규칙 (Communication & Commit Rules)
- **마크다운 서식 필수**: 모든 응답과 누적 커밋 메시지는 마크다운(Markdown) 서식(헤더 `#`, 볼드 `**`, 리스트, 코드 블록 등)을 적극적으로 활용하여 가독성 있게 작성할 것.
- **커밋 메시지 제공**: 작업 완료 후에는 언제든지 Git 커밋에 바로 복사하여 사용할 수 있도록 마크다운 코드 블록 형태로 커밋 메시지를 제공할 것.
- **한국어 응답**: 사용자와의 모든 소통은 정중하고 친절한 한국어로 진행할 것.

---

## 2. 인터랙션 및 UX 핵심 가이드라인

### 공통 (Common)
- **섹션 페이드인 등장 (Scroll Reveal)**:
  - 스크롤 시 각 섹션이 1/3 (약 33%) 이상 뷰포트에 들어오면 부드럽게 나타나도록 IntersectionObserver 설정.
- **하단 네비게이션 (Bottom Mini Navigation)**:
  - 네비게이션 버튼을 클릭하여 이동할 때는 부드럽게 스크롤하며, 스크롤 도중 현재 위치 표시가 버벅이지 않도록 딜레이(`isNavigatingViaClick`) 처리.
- **모바일 뒤로가기 제어 (History & Popstate)**:
  - 갤러리 스토리, 현장스냅 라이트박스, 업로드 모달 등 레이어 팝업이 떠 있는 상태에서 모바일 브라우저 뒤로가기 실행 시 이전 페이지(`about:blank` 등)로 이동하지 않고 **오직 레이어 팝업만 닫히도록** `history.pushState` 및 `popstate` 연동.
  - 레이어 팝업 종료 시 모달 진입 직전의 스크롤 위치(`savedScrollY`)를 즉시 및 `requestAnimationFrame` + `setTimeout`으로 오차 없이 완벽 복원.
  - 브라우저 자체의 스크롤 점프를 방지하기 위해 `window.history.scrollRestoration = 'manual'` 유지.

---

### 첫 번째 섹션 (CoverSection)
- 모바일 화면에 꽉 차는 풀스크린 레이아웃 유지.
- 컨텐츠 상하 간격이 너무 벌어지지 않도록 최적화된 패딩/마진 유지.

---

### 갤러리 섹션 (GallerySection)
- **레이아웃 붕괴 방지**:
  - `.gallery-grid`에 레이아웃 축소 및 스크롤 점프를 유발하는 `content-visibility: auto`를 절대 사용하지 말 것.
- **스켈레톤 로딩 (Skeleton Shimmer UI)**:
  - 3x3 썸네일 그리드와 전체화면 인스타그램 스토리 뷰어의 고해상도 메인 사진 모두 로딩 중 스켈레톤 쉬머 및 골드 스피너 로더 적용.
- **롱프레스 픽(Peek) 미리보기**:
  - 모바일에서 썸네일을 길게 누를 시 브라우저 기본 컨텍스트 메뉴 차단(`@contextmenu.prevent`), 배경 블러 오버레이와 함께 미리보기 카드 팝업.
- **스토리 재생 제어**:
  - 스토리 길게 누를 시 화면 일시정지(표시 문구 없이 영상/사진 멈춤).
  - 일시정지 상태에서 이전/다음 사진으로 이동 시 자동으로 일시정지가 해제되며 정상 재생 시작.

---

### 현장스냅 섹션 (LiveSnapSection)
- **자동 스크롤 및 사용자 인터랙션**:
  - 배경 메이슨리 피드는 끊김 없는 무한 자동 스크롤(translateY 루프)로 동작.
  - 사용자가 화면을 터치/스크롤하면 자동 스크롤이 즉시 멈추고, 조작이 끝난 후 5초 뒤에 자동 재개.
  - 사용자가 피드의 이미지나 동영상 카드를 클릭했을 때는 자동 스크롤을 즉시 일시정지(`isPaused = true`).
  - 사용자가 위/아래 끝까지 스크롤했을 때는 진동이나 과도한 문구 없이 깔끔한 끝 인디케이터(라인/핍) 표식만 노출.
- **실시간 업로드 & 일러스트 분배 (중복 방지)**:
  - 현장스냅 기능 OFF 시 갤러리 예시 사진 6장이 블러 배경으로 스크롤되며 내부 스크롤 차단.
  - 현장스냅 기능 ON 시 사용자가 올린 사진 우선 표시.
  - 남은 슬롯을 채우는 14종 웨딩 일러스트는 **홀수 풀(Pool 1: 7종)**과 **짝수 풀(Pool 2: 7종)**로 완전 분리 배분하여, 좌우 열 및 상하 스크롤 중 동일 이미지가 절대 겹치거나 중복 노출되지 않도록 할 것.
  - 사용자가 사진/영상 업로드 완료 후 확인창을 닫으면 현장스냅 섹션으로 부드럽게 스크롤 이동.
- **동영상 업로드 및 재생**:
  - Firebase Storage 업로드 시 비디오 `metadata: { contentType: file.type || 'video/mp4' }` 필수 지정.
  - 카드 내 비디오는 무음(`muted`), 인라인 자동 재생(`autoplay playsinline`), 맛보기 재생.
  - 라이트박스 오픈 시 소리와 함께 정상 재생(`lightboxVideoRef`를 통한 명시적 `play()`).
  - 라이트박스 이미지 및 비디오 로딩 시 스켈레톤 쉬머 및 스피너 로더 적용.

---

### 푸터 섹션 (ShareFooter)
- 직전 섹션(현장스냅)과의 시각적 구분감 유지.
- 불필요하게 화면 전체를 채우지 않고 일반적인 푸터보다 약간 여유 있는 적절한 높이감 유지.
